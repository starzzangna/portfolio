# 타이드스퀘어 프로젝트별 프론트엔드 면접 질의응답

> 기준 자료: 포트폴리오 `lib/site-data.ts`  
> 답변 방식: **상황 → 선택 → 구현 → 검증/성과** 순서로 30~60초 안에 설명한다.  
> 주의: 아래 답변은 포트폴리오에 적힌 사실을 중심으로 작성했다. 실제 코드·지표 산출 근거와 다른 표현은 면접 전에 반드시 수정한다.

---

## 0. 면접 전 추가 확인

- `40% 단축`, `12% 감소` 같은 성과는 비교 기간, 표본, 측정 도구를 준비한다. 정확한 로그가 없다면 “작업 티켓 기준 내부 추정”처럼 범위를 한정한다.

---

## 1. 프로모션 템플릿·배포 관리자

### Q1. 프론트엔드와 백엔드의 경계를 어떻게 나눴나요?

**A.** 프론트엔드는 브랜드 템플릿, Admin 정보 구조, 입력 검증, 작업 상태 표현과 사용자 상호작용을 담당했습니다. 백엔드는 tar 업로드 이후의 작업 큐, QA 캡처, 배포·롤백 같은 실행 책임을 가졌습니다. 두 영역이 상태 이름이나 payload를 각자 해석하지 않도록 Zod 스키마와 작업 상태 모델을 계약으로 맞췄습니다. 덕분에 프론트는 작업의 내부 구현을 몰라도 상태 전이와 오류를 일관되게 표현할 수 있었습니다.

### Q2. REST, SSE, polling을 함께 사용한 이유는 무엇인가요?

**A.** 과거 로그와 현재 상태는 REST로 먼저 받아 화면을 즉시 복원하고, 이후 이벤트는 SSE로 이어 붙였습니다. 다만 프록시 버퍼링이나 연결 지연으로 SSE 이벤트가 늦을 수 있어 일정 시간 변화가 없을 때만 마지막 이벤트 ID 이후를 polling했습니다. polling을 상시 사용하지 않아 요청량을 줄이면서도 실시간 화면이 멈춘 것처럼 보이는 문제를 보완했습니다. 재연결 시에는 중복 로그가 생기지 않도록 이벤트 ID 기준으로 병합하는 것이 핵심이었습니다.

### Q3. TanStack Query의 캐시는 어떻게 설계했나요?

**A.** 대시보드, 브랜드별 배포 목록, 작업 목록을 서로 다른 query key로 분리하고 브랜드·검색어·상태·페이지 같은 조회 조건을 key에 포함했습니다. 작업 상태가 바뀌면 관련 작업 상세뿐 아니라 목록과 대시보드도 선택적으로 invalidate했습니다. 실시간 갱신 중에는 모든 쿼리를 무조건 refetch하지 않고, 활성 작업만 짧은 간격으로 갱신하거나 SSE 이벤트로 캐시를 업데이트해 네트워크 비용과 화면 흔들림을 줄였습니다.

### Q4. tar 업로드 UX에서 어떤 예외를 처리했나요?

**A.** 클릭 선택과 drag-and-drop을 같은 제어형 필드로 처리하고 파일명, 용량, 확장자, 교체·제거 상태를 명확히 보여줬습니다. 운영체제의 파일 선택 필터만 신뢰하지 않고 선택 후 Zod로 `.tar`와 `.tar.gz`를 검증했습니다. 신규 배포 경로는 archive 내부 `/promo/{slug}` 구조에서 확인하고, 수정 배포의 path는 읽기 전용으로 고정해 사용자 입력과 실제 산출물 경로가 어긋나는 문제를 줄였습니다.

### Q5. 이 운영 화면의 접근성과 반응형은 어떻게 검증했나요?

**A.** 데스크톱에서는 정보 밀도가 높은 표, 모바일에서는 핵심 필드 중심 카드를 제공했습니다. 상태는 색만으로 구분하지 않고 텍스트와 아이콘을 함께 사용했으며, Dialog focus trap, 키보드 탐색, `focus-visible`, 상태 변경 안내를 확인했습니다. 애니메이션은 `prefers-reduced-motion`에 대응했고 390, 768, 1024, 1440px 구간에서 가로 overflow와 콘솔 오류를 점검했습니다.

---

## 2. AI Builder

### Q1. LLM이 만든 코드를 운영 저장소에 바로 쓰지 않은 이유는 무엇인가요?

**A.** LLM 출력은 비결정적이어서 경로 이탈, 위험 API 사용, 과도한 파일 크기, 브랜드 규칙 위반이 발생할 수 있습니다. 그래서 생성과 반영을 분리하고, 미리보기·코드 비교 후에 경로·확장자·용량·위험 패턴 검사를 통과한 파일만 큐에 넣었습니다. 워커는 허용된 루트 안에서만 쓰고 기존 파일을 먼저 백업했으며, 메타데이터로 생성 버전과 현재 파일의 드리프트를 확인해 덮어쓰기 충돌을 막았습니다.

### Q2. Sandpack 미리보기와 실제 Next.js 결과의 차이는 어떻게 다뤘나요?

**A.** 브라우저 미리보기는 빠른 피드백에 유리하지만 실제 앱의 서버 기능, 빌드 설정, 공통 레이아웃을 완전히 재현하지는 못합니다. 따라서 Sandpack은 UI 초안 검증에 사용하고, 반영 전에는 허용 import와 파일 구조를 검사한 뒤 실제 프로젝트 환경에서 빌드·렌더 검증을 별도로 수행했습니다. 미리보기 성공만으로 배포 가능하다고 판단하지 않고 실제 앱의 검증 결과를 반영 기준으로 삼았습니다.

### Q3. 생성 결과의 버전과 충돌은 어떻게 관리했나요?

**A.** 생성 턴마다 입력, 산출물 해시, 대상 경로, 기준 버전을 메타로 남겼습니다. 사용자가 미리보는 동안 운영 파일이 바뀌면 기준 해시와 현재 해시가 달라지므로 자동 덮어쓰지 않고 충돌로 처리했습니다. 반영 전 백업과 생성 턴별 diff를 제공해 문제가 생겼을 때 이전 상태로 복원하거나 변경 이유를 추적할 수 있게 했습니다.

### Q4. LLM 호출 실패 시 UX는 어떻게 유지했나요?

**A.** 생성 요청을 비동기 작업으로 보고 대기·생성·검증·실패 상태를 분리했습니다. 네트워크나 모델 오류가 발생하면 무한 재시도하지 않고 재시도 가능한 오류와 입력 수정이 필요한 오류를 구분했습니다. 기본 랜딩 구조는 규칙 기반 폴백 템플릿으로 만들 수 있게 해, 모델 장애가 전체 제작 흐름의 중단으로 이어지지 않도록 했습니다.

### Q5. 40% 단축이라는 성과는 어떻게 설명해야 하나요?

**A.** 기존 요청 접수부터 초안 전달까지의 평균 시간과 빌더 도입 후 같은 유형 작업의 시간을 비교했다고 설명할 수 있어야 합니다. 개발자 코딩 시간, 피드백 횟수, 실제 운영 반영 시간을 각각 분리하면 더 설득력이 있습니다. 정확한 로그가 없다면 절대적인 생산성 지표처럼 말하지 않고 제한된 내부 작업 표본의 추정치라고 밝히겠습니다.

---

## 3. TideBuilder — Figma to Email HTML

### Q1. 일반 웹이 아니라 테이블 기반 이메일 HTML을 생성한 이유는 무엇인가요?

**A.** 이메일 클라이언트는 최신 CSS 지원 수준이 다르고 특히 Outlook은 Word 렌더링 엔진의 제약이 있습니다. 그래서 시맨틱한 웹 레이아웃보다 테이블, 인라인 스타일, 명시적 너비처럼 호환성이 검증된 방식을 우선했습니다. 생성 결과는 Gmail과 Outlook의 공통 지원 범위에 맞추고, 브랜드 헤더·푸터와 CTA 링크도 템플릿에서 일관되게 주입했습니다.

### Q2. Figma 레이어를 어떤 기준으로 HTML로 변환했나요?

**A.** 레이어 이름에 컷, 버튼, 링크 같은 제작 규칙을 두고 프레임 트리를 섹션과 CTA로 정규화했습니다. 버튼 행을 기준으로 이미지를 나눠 클릭 가능한 셀 구조를 만들고, 링크 영역은 별도 anchor로 매핑했습니다. 디자이너와 네이밍 규칙을 합의했기 때문에 시각적 좌표를 무조건 추론하는 방식보다 결과가 예측 가능하고 수정도 쉬웠습니다.

### Q3. Figma API 호출 제한은 어떻게 대응했나요?

**A.** 요청 동시성을 제한하고 지수 백오프를 적용해 일시적인 제한을 흡수했습니다. REST API로 프레임을 가져오기 어려운 경우에는 Figma 플러그인이 필요한 데이터를 직접 전달하는 폴백 경로를 제공했습니다. 두 입력 경로는 이후 파싱·슬라이싱 파이프라인을 공유해 결과가 달라지지 않도록 했습니다.

### Q4. 대용량 업로드 실패는 어떻게 해결했나요?

**A.** 큰 이미지 데이터를 JSON에 포함해 애플리케이션 서버로 보내는 방식은 요청 본문 제한과 메모리 사용량 문제가 있었습니다. 데이터를 청크로 나누거나 압축하고, 최종적으로는 서명 URL을 이용해 브라우저가 스토리지로 직접 업로드하게 했습니다. 애플리케이션 서버에는 업로드된 객체 URL과 메타데이터만 전달해 서버 부하와 실패 지점을 줄였습니다.

---

## 4. 패키지 맞춤여행 견적 문의 게시판

### Q1. Server Action을 사용한 이유와 주의점은 무엇인가요?

**A.** 회원 쿠키와 내부 API 호출을 브라우저에 노출하지 않고 서버에 모으기 위해 사용했습니다. Server Action 자체를 권한 경계로 보지 않고 매 요청에서 회원 세션과 문의 소유권 또는 비회원 비밀번호를 다시 확인했습니다. 오류는 사용자 입력 오류, 인증 오류, 서버 오류로 구분해 폼에 반환하고 개인정보는 로그나 클라이언트 복원 데이터에 남기지 않았습니다.

### Q2. 회원·비회원 접근 제어는 어떻게 설계했나요?

**A.** 로그인 여부와 글의 작성 유형을 섞은 조건문으로 처리하지 않고, 회원 본인·타 회원·비회원 인증 완료·미인증 같은 명시적 상태로 나눴습니다. 목록에는 최소 정보만 노출하고 상세 API는 서버에서 소유권이나 비회원 인증 상태를 다시 검사했습니다. 프론트의 모달과 리다이렉트는 UX만 담당하고 최종 권한 판단은 서버에 두었습니다.

### Q3. 브라우저 세션의 AES 비밀번호 보관은 안전한가요?

**A.** 충분한 보안 경계는 아닙니다. 암호문과 키를 모두 클라이언트가 알 수 있으면 XSS나 개발자 도구 접근을 막을 수 없기 때문입니다. 당시에는 같은 탭에서 1시간 재인증을 줄이기 위한 UX 수단이었고, 더 안전한 설계는 서버 검증 후 짧은 만료의 opaque 토큰을 발급해 `HttpOnly`, `Secure`, `SameSite` 쿠키로 저장하는 것입니다. 비밀번호 자체는 서버에서도 단방향 해시로 비교해야 합니다.

### Q4. PC 페이징과 모바일 더보기를 한 API로 어떻게 처리했나요?

**A.** 서버 API의 page와 size 계약은 공유하되, PC는 페이지 단위로 결과를 교체하고 모바일은 다음 페이지를 기존 목록에 누적했습니다. 필터나 검색어가 바뀌면 모바일 누적 목록과 페이지 cursor를 함께 초기화했습니다. 중복 요청과 중복 항목을 막기 위해 요청 상태를 잠그고 문의 ID 기준으로 병합했습니다.

### Q5. 긴 작성 폼의 사용성을 어떻게 높였나요?

**A.** 필드별 즉시 검증과 제출 시 전체 검증을 조합하고, 첫 오류 필드로 스크롤한 뒤 포커스를 이동했습니다. 금액·전화번호는 표시값과 전송값을 분리했고 인원·날짜처럼 복합 입력은 모달에서 완료 조건을 명확히 했습니다. 장애 복원 데이터에는 여행 조건만 넣고 연락처·이메일·비밀번호 같은 개인정보는 제외했습니다.

---

## 5. 투어비스 USJ AI 챗봇 UI

### Q1. FAQ와 AI 응답을 결합한 이유는 무엇인가요?

**A.** 빈도가 높은 질문은 검증된 FAQ가 더 빠르고 일관되지만, 모든 자유 질의를 미리 작성할 수는 없습니다. 먼저 정규화된 질문을 FAQ와 매칭해 즉시 답하고, 기준 점수에 미달하면 AI 스트리밍으로 전환했습니다. 이렇게 하면 체감 응답 속도와 답변 범위를 함께 확보하면서, 가격·운영 정책처럼 정확성이 중요한 내용은 관리된 답변을 우선할 수 있습니다.

### Q2. 스트리밍 응답 상태는 어떻게 관리했나요?

**A.** 사용자 메시지, 서버 접수, 스트림 수신, 완료, 실패를 별도 상태로 두고 요청 중에는 중복 전송을 막았습니다. chunk는 완성된 문장이라고 가정하지 않고 버퍼에 누적해 이벤트 단위로 파싱했습니다. 연결이 끊기면 이미 받은 답변을 유지한 채 재시도 가능 상태를 보여주고, 세션 ID로 대화 맥락을 이어가도록 했습니다.

### Q3. 스트리밍 텍스트를 렌더링할 때 보안상 주의할 점은 무엇인가요?

**A.** 모델 출력을 그대로 `dangerouslySetInnerHTML`로 넣으면 XSS 위험이 있어 일반 텍스트는 escape하고 볼드·줄바꿈처럼 허용한 마크업만 파서로 변환했습니다. 링크는 출력 대상에서 제외했으며, 향후 허용한다면 `http/https` 프로토콜 allowlist와 `rel="noopener noreferrer"`를 함께 적용해야 합니다.

### Q4. PC와 모바일 UX를 왜 다르게 구성했나요?

**A.** PC에서는 원래 상품 페이지 맥락을 유지하도록 우하단 패널을 사용했고, 모바일에서는 작은 화면에서 대화와 키보드에 집중할 수 있도록 풀스크린으로 전환했습니다. 모바일 오픈 시 배경 스크롤을 잠그고 닫을 때 기존 위치를 복원했으며, 가상 키보드와 safe area를 고려했습니다. UI는 달라도 메시지·세션 로직은 공통 컴포넌트에서 공유했습니다.

---

## 6. 마케팅 Vercel 앱 프록시 및 SEO

### Q1. redirect가 아니라 rewrite 프록시를 사용한 이유는 무엇인가요?

**A.** redirect는 브라우저 주소가 독립 앱 도메인으로 바뀌어 브랜드 URL, 분석, 캐노니컬 일관성이 깨질 수 있습니다. rewrite는 사용자가 본 도메인 경로를 유지한 채 내부적으로 독립 Vercel 앱의 응답을 받게 합니다. 캠페인 앱은 독립적으로 배포하면서도 외부에는 하나의 도메인 구조를 제공할 수 있었습니다.

### Q2. 프록시 구조에서 정적 자산 경로 문제는 어떻게 처리하나요?

**A.** HTML만 rewrite하면 `/_next/static`, 이미지, API 경로가 원본 앱 기준으로 요청되어 충돌할 수 있습니다. 앱별 `basePath`나 asset prefix를 명확히 하거나, 캠페인 접두어 아래 자산 요청도 같은 origin으로 전달하도록 라우팅 규칙을 맞춰야 합니다. 쿠키 범위, CORS, 캐시 헤더, 404 처리도 HTML 경로와 함께 검증해야 합니다.

### Q3. 중복 콘텐츠와 캐노니컬 문제는 어떻게 방지했나요?

**A.** 실제 접근 가능한 Vercel 기본 도메인과 본 도메인에 같은 콘텐츠가 노출될 수 있으므로 본 도메인 URL을 canonical로 고정했습니다. 가능하면 원본 도메인의 인덱싱을 제한하고 sitemap에는 공개할 본 도메인 경로만 포함했습니다. OG URL, 구조화 데이터, 내부 링크도 같은 URL 생성 유틸을 사용해 서로 다른 도메인이 섞이지 않게 했습니다.

### Q4. SEO 메타를 어떻게 표준화했나요?

**A.** 브랜드 공통 title template, description, OG, robots 기본값을 두고 캠페인 경로별 설정만 덮어쓰는 병합 구조를 만들었습니다. 필수 메타 누락과 중복 title을 검사하고 sitemap 생성도 같은 경로 설정을 참조하게 했습니다. AI는 초안과 반복 작업을 돕는 수단으로 사용하되 키워드 적합성, 사실성, canonical은 사람이 검토했습니다.

---

## 7. 투어비스 고객센터 — Zendesk Guide

### Q1. Zendesk 테마의 레거시 JavaScript를 어떻게 개선했나요?

**A.** 전면 프레임워크 교체보다 영향 범위를 줄이는 점진적 개선을 선택했습니다. 인증, 예약 조회, 로더, 오류, 모달, 무한 스크롤을 명확한 함수형 인터페이스로 분리하고 DOM 접근 지점을 모았습니다. 기능별 초기화와 이벤트 해제를 대칭으로 관리해 중복 바인딩을 줄였고, 반복되던 레거시 코드를 공통 컴포넌트로 정리했습니다.

### Q2. 예약 기반 문의 흐름은 어떻게 구현했나요?

**A.** 로그인 상태에서 상품군별 예약을 조회하고 사용자가 예약을 선택하면 카테고리, 섹션, 예약번호를 문의 폼 딥링크에 포함했습니다. 폼 진입 후에는 해당 값을 검증해 상담 컨텍스트로 채웠습니다. URL 값만 신뢰하지 않고 접수 서버에서 로그인 세션과 예약 소유권을 다시 확인해 다른 사용자의 예약번호가 주입되는 것을 막았습니다.

### Q3. 무한 스크롤에서 어떤 문제를 고려했나요?

**A.** 모달 하단 sentinel을 Intersection Observer로 감지하고 다음 페이지가 있을 때만 요청했습니다. 빠른 스크롤의 중복 요청을 막고 예약 ID로 결과를 중복 제거했습니다. 로딩·빈 결과·재시도 상태를 분리하고, 모달을 닫을 때 observer와 이벤트를 해제해 누수와 중복 호출을 방지했습니다.

### Q4. 웹과 앱 WebView 차이는 어떻게 다뤘나요?

**A.** user agent만으로 판단하지 않고 앱에서 제공하는 bridge 가능 여부를 우선 확인했습니다. 로그인, 외부 링크, 뒤로 가기, 채팅 진입처럼 앱 동작이 필요한 기능은 adapter로 감싸고 웹에서는 URL 이동으로 폴백했습니다. GA 이벤트 이름과 payload는 공통으로 유지해 채널별 지표를 비교할 수 있게 했습니다.

---

## 8. 다음 쇼핑 제휴 랜딩 — CMS 템플릿 섹션 시스템

### Q1. CMS 응답을 바로 컴포넌트에 전달하지 않은 이유는 무엇인가요?

**A.** CMS 스키마가 UI 세부 구조에 직접 퍼지면 필드 변경 시 모든 컴포넌트를 수정해야 합니다. 템플릿별 adapter가 원본 응답을 공통 UI 모델로 정규화하고, 화면은 그 모델만 받도록 했습니다. 잘못된 템플릿이나 필수 필드 누락은 경계에서 검증해 특정 섹션 오류가 전체 페이지 장애로 번지지 않게 했습니다.

### Q2. 템플릿 레지스트리는 어떻게 확장했나요?

**A.** 템플릿 ID와 렌더 컴포넌트, 데이터 변환기, fallback을 하나의 레지스트리로 관리했습니다. 신규 섹션은 기존 조건문을 수정하기보다 레지스트리에 항목을 추가하도록 했습니다. 번들 크기가 큰 섹션은 동적 import를 적용할 수 있고, 알 수 없는 ID는 로깅 후 안전하게 건너뛰도록 구성했습니다.

### Q3. Intersection Observer 지연 렌더링의 단점은 무엇인가요?

**A.** 모든 하단 콘텐츠를 늦게 그리면 검색 엔진이나 빠른 스크롤 사용자가 빈 영역을 볼 수 있고 레이아웃 이동도 생깁니다. 첫 화면과 SEO에 중요한 콘텐츠는 서버에서 즉시 렌더하고, 무거운 상호작용이나 이미지가 많은 하위 섹션만 지연했습니다. 예상 높이 placeholder와 충분한 root margin으로 진입 전에 로드를 시작했습니다.

### Q4. ISR은 어떤 기준으로 사용했나요?

**A.** 제휴 랜딩은 매 요청마다 최신일 필요는 없지만 운영 변경이 일정 시간 안에는 반영되어야 해서 주기적 재검증이 적합했습니다. 가격·재고처럼 더 민감한 정보는 클라이언트 재조회나 짧은 캐시를 별도로 적용해야 합니다. 운영자가 즉시 반영해야 하는 경우에는 on-demand revalidation을 연결하는 것이 더 적절합니다.

---

## 9. airspot 백오피스 UI/UX 안정화

### Q1. 기존 플러그인 스타일 충돌을 어떻게 조사했나요?

**A.** 깨지는 화면에서 computed style과 적용 순서를 확인해 전역 selector, breakpoint, 높은 specificity 중 원인을 분리했습니다. 플러그인 전체 CSS를 덮어쓰기보다 백오피스 루트 scope와 CSS Module 안에서 필요한 규칙만 보정했습니다. 수정 전후 주요 화면을 PC와 모바일에서 비교해 다른 컴포넌트로 회귀하지 않는지 확인했습니다.

### Q2. JavaScript 디바이스 분기와 CSS media query 중 무엇을 선택하나요?

**A.** 단순 배치와 노출은 CSS media query가 기본입니다. 같은 콘텐츠를 화면 크기에 따라 표현만 바꾸는 경우 JS 분기는 hydration 불일치와 resize 상태 관리 비용을 만들 수 있기 때문입니다. 데이터 요청이나 상호작용 자체가 완전히 다를 때만 JS 분기를 사용하고, 가능하면 서버와 첫 렌더 결과가 일치하도록 설계합니다.

### Q3. CSS 소스가 3% 늘어난 것이 좋은 성과인가요?

**A.** 증가 자체는 성과가 아닙니다. 기존 구조를 전면 교체하지 않고 격리된 보정 스타일만 추가해 레이아웃 결함을 해결했다는 변경 범위 지표에 가깝습니다. 면접에서는 회귀 위험과 작업 비용을 제한한 의사결정으로 설명하고, 장기적으로는 플러그인 의존성 제거와 디자인 토큰 통합이 필요하다고 말하겠습니다.

---

## 10. 투어비스 마이페이지 리뉴얼

### Q1. Server Component와 Client Component를 어떻게 나눴나요?

**A.** 회원 확인, 초기 예약·쿠폰·이용권 데이터 조회와 정적인 레이아웃은 서버 컴포넌트에서 처리했습니다. 탭, 필터, 추가 이력 API 조회, 무한 스크롤, 아코디언, 모달처럼 브라우저 상태와 이벤트가 필요한 부분은 클라이언트 컴포넌트로 분리했습니다. 초기 콘텐츠는 서버에서 완성하고 후속 API 연동과 인터랙션만 클라이언트가 담당하도록 경계를 나눴습니다.

### Q2. 상품군별로 다른 예약 카드를 어떻게 공통화했나요?

**A.** 모든 상품을 하나의 거대한 조건부 컴포넌트로 만들지 않고, 공통 카드 shell과 상품군별 adapter를 분리했습니다. adapter가 예약번호, 상태, 일정, CTA를 공통 view model로 변환하고 국내선 환불이나 복수 예약번호 같은 예외는 명시적인 variant로 표현했습니다. 공통 레이아웃은 유지하면서 상품별 정책 변경의 영향 범위를 제한했습니다.

### Q3. 쿠폰 뱃지 정책을 결제 화면과 어떻게 동기화했나요?

**A.** 선착순, 잔여 수량, 마감 임박 조건을 각 화면에서 다시 구현하지 않고 순수 함수와 공통 타입으로 분리했습니다. 쿠폰함과 결제 모달이 같은 입력으로 같은 label과 우선순위를 계산하도록 했습니다. 경계값을 테이블 테스트로 검증하면 날짜·수량 조건 변경 시 두 화면의 불일치를 예방할 수 있습니다.

### Q4. 무한 스크롤 접근성은 어떻게 보완하나요?

**A.** 자동 로드만 제공하면 키보드와 스크린리더 사용자가 결과 추가를 인지하기 어렵습니다. “더 보기” 버튼을 기본 조작으로 제공하고 Intersection Observer는 이를 자동 실행하는 보조 수단으로 사용할 수 있습니다. 추가된 항목 수는 live region으로 안내하고, 로딩 중 버튼 상태와 목록 종료를 명확히 표시합니다.

---

## 11. 투어비스 공통 메인·GNB 리뉴얼

### Q1. GNB를 공유 패키지로 만든 이유는 무엇인가요?

**A.** 항공·호텔·패키지 등 앱마다 내비게이션을 구현하면 정책 변경 때 중복 작업과 버전 불일치가 생깁니다. UI, 타입, 데이터 정규화, 이벤트 계약을 공유 패키지로 묶어 한 번의 변경을 여러 앱에 전파했습니다. 다만 강제 일괄 업데이트 위험이 있어 semantic version, 변경 이력, 소비 앱별 회귀 테스트가 필요합니다.

### Q2. 환경마다 다른 메뉴 데이터를 어떻게 안정화했나요?

**A.** CMS 원본을 UI에서 직접 읽지 않고 정규화 계층에서 공통 메뉴 모델로 변환했습니다. 누락 필드는 기본값을 적용하고 URL, 메뉴 타입, children 구조를 런타임 검증했습니다. 이후 렌더러는 단순 링크, 드롭다운, 메가메뉴, 캐스케이드 같은 UX 타입만 보고 그리도록 해 환경 차이를 UI 밖에서 흡수했습니다.

### Q3. 서버와 클라이언트의 역할은 어떻게 분리했나요?

**A.** Next.js App Router에서 feature flag, 회원 상태, 메뉴 원본처럼 요청 시 결정되는 값은 서버에서 준비했습니다. hover, focus, 모달, 모바일 메뉴처럼 사용자 이벤트가 필요한 작은 영역만 클라이언트 컴포넌트로 분리했습니다. 전체 GNB를 클라이언트 컴포넌트로 만들지 않아 hydration 범위를 줄이고 첫 화면 구조를 안정적으로 제공했습니다.

### Q4. 동일 우선순위 배너 셔플 시 hydration 불일치는 없었나요?

**A.** 서버와 클라이언트가 각각 `Math.random()`을 실행하면 순서가 달라질 수 있습니다. 서버에서 순서를 확정해 전달하거나 날짜·세션 기반 seed로 결정적 셔플을 해야 합니다. 노출 공정성을 정확히 측정하려면 셔플만 적용할 것이 아니라 impression 이벤트와 사용자 단위 중복 노출도 함께 확인해야 합니다.

### Q5. 공유 패키지 배포의 장애 전파 위험은 어떻게 줄이나요?

**A.** 컴포넌트 단위 테스트와 Storybook 시각 검증, 소비 앱 smoke test를 거친 뒤 canary 버전을 특정 앱에 먼저 적용할 수 있습니다. breaking change는 major 버전으로 분리하고 앱이 한꺼번에 자동 업데이트되지 않도록 lockfile을 유지합니다. 런타임 오류 시 최소 내비를 보여주는 fallback도 필요합니다.

---

## 12. 투어비스 항공 리뉴얼 퍼블리싱

### Q1. 디자인 시안 없이 구현 기준을 어떻게 만들었나요?

**A.** 기존 운영 화면을 페이지가 아니라 헤더, 검색, 여정 카드, 운임, 상태 UI 같은 패턴 단위로 분해했습니다. 운영 담당자와 유지해야 할 정보 우선순위와 변경 가능한 범위를 확인하고, 대표 화면을 먼저 만들어 기준으로 합의했습니다. 이후 반응형 규칙과 상태별 예외를 문서화해 화면마다 추측해서 구현하는 일을 줄였습니다.

### Q2. FE 개발자가 사용하기 쉬운 퍼블리싱 산출물은 무엇인가요?

**A.** DOM이 데이터 구조와 자연스럽게 대응하고, 반복 요소와 상태 class가 예측 가능해야 합니다. 고정 문구나 인라인 스타일을 피하고 컴포넌트 경계, 빈 상태, 로딩, 오류, 긴 텍스트까지 샘플을 제공합니다. 이벤트 구현에 필요한 버튼과 입력의 의미를 시맨틱 요소와 명확한 이름으로 전달하는 것도 중요합니다.

### Q3. 항공 검색 UI의 접근성에서 중요한 점은 무엇인가요?

**A.** 출발지·도착지, 날짜, 인원, 좌석 등 복합 입력의 label과 오류 연결이 중요합니다. 모달이나 달력은 키보드 이동, focus 복원, 현재 선택 상태를 제공해야 합니다. 운임과 조건을 색만으로 구분하지 않고 텍스트로 표시하며, 동적 검색 결과 갱신을 스크린리더에 알릴 수 있어야 합니다.

---

## 13. 투어비스 리뉴얼 퍼블리싱

### Q1. 모바일 퍼스트를 선택한 이유는 무엇인가요?

**A.** 핵심 콘텐츠와 조작을 작은 화면에서 먼저 우선순위화한 뒤 넓은 화면에서 레이아웃을 확장하기 위해서입니다. 기본 스타일을 모바일에 두고 `min-width` breakpoint로 컬럼, 여백, 보조 정보를 추가하면 중복 override를 줄일 수 있습니다. 단순히 모바일 화면을 늘리는 것이 아니라 데스크톱의 hover와 넓은 정보 밀도도 별도로 검증했습니다.

### Q2. 페이지 단일화로 소스 40%를 줄였다는 의미는 무엇인가요?

**A.** PC·모바일 페이지를 별도 마크업으로 유지하던 구조를 하나의 의미 구조와 반응형 스타일로 합친 결과라고 설명할 수 있습니다. 공통화가 목적이 되어 지나친 조건문이 생기지 않도록 콘텐츠와 동작이 같은 영역만 합치고, 정말 다른 UX는 variant로 분리했습니다. 줄 수보다 중복 수정 지점과 회귀 범위가 줄었다는 점이 더 중요한 성과입니다.

### Q3. 반응형 breakpoint는 어떻게 정했나요?

**A.** 특정 기기 이름보다 콘텐츠가 깨지는 지점을 기준으로 정했습니다. 검색 폼, 카드 최소 너비, 내비게이션 공간처럼 레이아웃 제약을 확인해 breakpoint를 만들고, 중간 너비와 확대 환경에서도 검증했습니다. CSS pixel 기준이므로 고해상도 기기 목록만 보고 정하지 않았습니다.

---

## 14. 투어비스 호텔 메인 UI 리뉴얼

### Q1. 페이지를 얇게 유지하고 섹션을 조립한 이유는 무엇인가요?

**A.** 페이지가 CMS 데이터 파싱과 각 섹션 표현까지 모두 담당하면 템플릿 추가 때 수정 범위가 커집니다. 페이지는 데이터 조회와 섹션 순서만 결정하고, 템플릿 ID를 섹션 컴포넌트에 매핑했습니다. 배너, 상품 그리드, 타임딜 같은 섹션은 독립적으로 변경·검증할 수 있어 캠페인 변경 공수를 줄였습니다.

### Q2. 첫 화면과 하단 섹션의 로딩 전략은 어떻게 달랐나요?

**A.** 상단 내비와 첫 콘텐츠는 즉시 렌더하고 크기가 정해진 스켈레톤으로 레이아웃 이동을 줄였습니다. 퀵서치·최근 본 상품처럼 개인화되거나 무거운 영역과 하단 섹션은 지연 로드했습니다. root margin을 둔 Intersection Observer로 실제 진입 전에 준비하고 이미지 크기를 예약해 CLS를 줄였습니다.

### Q3. 서버 조립과 클라이언트 상호작용의 경계는 무엇이었나요?

**A.** Next.js Pages Router의 SSR 단계에서 CMS 데이터를 조회하고 템플릿 ID에 맞는 섹션과 기본 마크업을 조립했습니다. 탭·검색·드롭다운·Swiper처럼 상태나 브라우저 API가 필요한 기능은 CSR로 처리했습니다. 서버에서 초기 콘텐츠를 완성해 첫 화면과 SEO를 확보하면서, 상호작용 코드만 브라우저에서 실행하도록 역할을 나눈 구조입니다.

### Q4. 캠페인을 탭과 배지만으로 전환한 장점은 무엇인가요?

**A.** 별도 페이지를 복제하지 않고 기존 정보 구조 안에서 기본 탭, 배지, 일부 콘텐츠 설정만 바꿔 배포 범위를 제한할 수 있었습니다. 캠페인 종료 후 원복도 설정 중심으로 가능했습니다. 다만 캠페인 상태가 URL에 없으면 공유와 분석이 어려울 수 있어 필요하면 query나 명시적 경로를 사용해야 합니다.

---

## 15. 투어비스 Publish — Gulp 빌드 환경 표준화

### Q1. 당시 Gulp를 선택한 이유는 무엇인가요?

**A.** 서비스별 정적 퍼블리싱 산출물에 SCSS 컴파일, HTML include, JS 압축, 이미지 최적화, 로컬 서버가 필요했고 이를 빠르게 통일하기에 적합했습니다. 애플리케이션 번들러를 새로 도입하는 것보다 기존 산출물 구조를 유지하면서 반복 작업을 자동화할 수 있었습니다. 현재라면 Vite나 프레임워크 빌드와 비교하겠지만, 당시 문제와 팀의 학습 비용을 기준으로 선택했습니다.

### Q2. Gulp 4 태스크를 어떻게 구성했나요?

**A.** 스타일, 마크업, 스크립트, 이미지 태스크를 독립적으로 만들고 선행 정리 후 가능한 작업은 `parallel`, 의존 작업은 `series`로 실행했습니다. 각 태스크는 stream을 반환해 종료 시점을 Gulp가 알 수 있게 했고 개발 watch와 production build를 분리했습니다. 에러가 발생했는데도 성공한 산출물처럼 보이지 않도록 오류 전파도 명확히 했습니다.

### Q3. 변경된 이미지만 최적화한 이유는 무엇인가요?

**A.** 전체 이미지를 매번 압축하면 개발 피드백과 CI 시간이 크게 늘어납니다. 파일 변경 시각이나 캐시를 기준으로 변경분만 처리하고, 클린 빌드에서는 전체를 검증했습니다. 속도 최적화가 오래된 산출물을 남기지 않도록 삭제된 파일 동기화와 캐시 초기화 경로도 함께 마련해야 합니다.

### Q4. HTML partial include의 장점과 한계는 무엇인가요?

**A.** 공통 헤더·푸터의 중복과 수정 누락을 줄이고 정적 산출물을 유지할 수 있다는 장점이 있습니다. 반면 타입 검사, 복잡한 상태, 중첩 데이터 표현에는 한계가 있고 include 의존성이 깊어지면 추적이 어렵습니다. 그래서 단순 공통 마크업에 제한하고 애플리케이션 로직이 필요한 영역은 프론트엔드 컴포넌트로 옮기는 기준이 필요합니다.

---

## 16. 프로젝트 공통 심층 질문

### Q1. 혼자 프론트엔드를 담당한 프로젝트에서 코드 품질을 어떻게 보장했나요?

**A.** 혼자 구현해도 요구사항·API 계약·상태 모델을 문서화하고, lint·type check·빌드·핵심 플로우 테스트를 완료 기준에 포함했습니다. 위험한 변경은 작은 단위로 나누고 기획·디자인·백엔드와 중간 결과를 자주 검증했습니다. 운영 로그와 사용자 피드백을 배포 후 확인해 혼자 판단한 UX를 보정했습니다.

### Q2. 재사용성과 프로젝트별 요구사항이 충돌할 때 어떻게 판단하나요?

**A.** 시각적으로 비슷하다는 이유만으로 바로 공통화하지 않습니다. 데이터 구조, 상호작용, 변경 주기가 함께 같은지 확인하고, 공통 shell과 정책별 adapter·variant를 분리합니다. 조건 prop이 계속 늘거나 한 프로젝트 변경이 다른 프로젝트 회귀를 만들면 공통화 경계가 잘못됐다는 신호로 봅니다.

### Q3. 성능 최적화는 어떤 순서로 하나요?

**A.** 먼저 실제 사용자 지표나 프로파일링으로 병목을 확인합니다. 그다음 불필요한 요청과 JavaScript, 큰 이미지, 렌더 차단 리소스, 반복 렌더 순서로 영향이 큰 항목을 줄입니다. 지연 로딩 자체를 목표로 삼지 않고 LCP, INP, CLS와 업무 화면의 체감 대기 시간이 개선됐는지 변경 전후를 비교합니다.

### Q4. 프론트엔드 오류 처리는 어떻게 설계하나요?

**A.** 입력 오류, 인증·권한 오류, 일시적 네트워크 오류, 서버 오류를 구분합니다. 사용자가 해결할 수 있는 오류는 해당 필드나 행동 근처에 안내하고, 재시도 가능한 요청은 중복 실행에 안전한지 확인한 뒤 재시도합니다. 예상하지 못한 오류는 error boundary와 모니터링으로 수집하되 개인정보와 토큰은 로그에서 제거합니다.

### Q5. 접근성을 프로젝트 일정 안에서 어떻게 확보했나요?

**A.** 마지막 QA 항목이 아니라 컴포넌트 기본 조건으로 넣었습니다. 시맨틱 요소, label, 키보드 조작, focus, 색상 외 상태 표현을 공통 UI 단계에서 처리하고 화면 개발자는 예외만 확인하게 했습니다. 자동 검사만 믿지 않고 주요 여정을 키보드와 스크린리더 기준으로 수동 점검했습니다.

### Q6. 본인의 기여도를 어떻게 증명할 수 있나요?

**A.** “프론트엔드 1명”이라는 인원 설명보다 제가 결정한 구조, 직접 구현한 범위, 다른 직군과 합의한 계약, 배포 후 지표를 분리해 말하겠습니다. 팀 성과는 팀 성과로 표현하고, 코드·문서·이슈·대시보드처럼 확인 가능한 근거를 준비하겠습니다.

---

## 17. 답변 마무리에 사용할 문장

- “당시에는 일정과 기존 구조를 고려해 이 방식을 선택했고, 지금 다시 한다면 ○○까지 보완하겠습니다.”
- “프론트의 화면 분기만으로 권한을 보장하지 않고 서버에서 소유권을 다시 검증했습니다.”
- “공통화 자체가 목적이 아니라 변경 지점과 회귀 범위를 줄이는 것을 기준으로 삼았습니다.”
- “성과 수치는 ○○ 기간의 작업 티켓/분석 이벤트를 기준으로 비교했습니다.”
- “제가 직접 결정하고 구현한 범위는 ○○이고, 백엔드/기획과 합의한 경계는 ○○입니다.”
