# 타이드스퀘어 프론트엔드 면접 질의응답 Tree

> 각 기본 질문 아래에 답변에서 이어질 가능성이 높은 파생 질문과 답변을 배치했다.  
> 기본 답변은 30~60초, 파생 답변은 20~30초 안에 말하는 것을 기준으로 한다.  
> 성과 수치는 실제 비교 기간·표본·측정 근거를 확인한 뒤 사용한다.

## 1. 프로모션 템플릿·배포 관리자

- **Q1. 프론트엔드와 백엔드의 경계를 어떻게 나눴나요?**
  - **답변:** 프론트엔드는 브랜드 템플릿, Admin 정보 구조, 입력 검증과 작업 상태 표현을 담당하고, 백엔드는 작업 큐, QA 캡처, 배포·롤백 실행을 담당했습니다. 양쪽은 공통 Zod 스키마와 작업 상태 모델을 API 계약으로 사용했습니다.
  - **파생 Q1-1. 공통 스키마가 변경되면 어떻게 대응했나요?**
    - **답변:** 필드 추가는 선택값과 기본값을 활용해 하위 호환성을 유지하고, 상태명이나 필수 필드 변경은 계약 버전과 함께 반영했습니다. 프론트에서는 알 수 없는 상태를 fallback으로 표시해 전체 화면이 깨지지 않게 했습니다.

- **Q2. REST, SSE, polling을 함께 사용한 이유는 무엇인가요?**
  - **답변:** 과거 로그와 현재 상태는 REST로 복원하고 이후 이벤트는 SSE로 받았습니다. 프록시 버퍼링 등으로 이벤트가 지연될 때만 마지막 이벤트 ID 이후를 polling해 실시간성과 복구 가능성을 함께 확보했습니다.
  - **파생 Q2-1. SSE 재연결 시 중복 로그는 어떻게 막았나요?**
    - **답변:** 마지막으로 처리한 이벤트 ID를 보관하고 재연결·polling 결과를 ID 기준으로 병합했습니다. 이미 처리한 이벤트는 제외하고 누락된 구간만 추가했습니다.

- **Q3. TanStack Query 캐시는 어떻게 설계했나요?**
  - **답변:** 대시보드, 브랜드별 배포, 작업 목록·상세를 분리하고 브랜드·검색어·상태·페이지를 query key에 포함했습니다. 상태 변경 시 영향을 받는 목록만 invalidate하고 활성 작업만 짧게 갱신했습니다.
  - **파생 Q3-1. SSE 이벤트로 캐시를 직접 수정하는 것과 invalidate의 기준은 무엇인가요?**
    - **답변:** 이벤트 payload만으로 완전한 데이터를 만들 수 있으면 `setQueryData`로 즉시 반영하고, 여러 집계나 정렬 결과가 함께 바뀌면 invalidate해 서버 값을 다시 조회했습니다.

- **Q4. tar 업로드 UX에서 어떤 예외를 처리했나요?**
  - **답변:** 클릭과 drag-and-drop을 함께 지원하고 파일명, 용량, 확장자, 교체·제거 상태를 한 필드에서 보여줬습니다. 선택 후 Zod로 파일을 검증하고 신규 slug는 archive 구조에서 확인했습니다.
  - **파생 Q4-1. 프론트 검증만으로 업로드 안전성을 보장할 수 있나요?**
    - **답변:** 보장할 수 없습니다. 프론트 검증은 빠른 피드백을 위한 것이고 서버에서 용량, MIME, archive 경로 이탈, 허용 파일과 악성 콘텐츠를 다시 검사해야 합니다.

- **Q5. 운영 화면의 접근성과 반응형은 어떻게 검증했나요?**
  - **답변:** 데스크톱은 표, 모바일은 핵심 정보 중심 카드로 구성했습니다. 상태를 색과 텍스트로 함께 표현하고 키보드 탐색, focus, Dialog, reduced motion과 주요 화면 폭의 overflow를 확인했습니다.
  - **파생 Q5-1. 표와 모바일 카드가 같은 정보를 유지하는 방법은 무엇인가요?**
    - **답변:** 동일한 view model을 사용하고 표현 컴포넌트만 분리했습니다. 상태명, 날짜, 작업 액션의 계산 로직을 공통화해 디바이스별 정책 차이가 생기지 않게 했습니다.

## 2. AI Builder

- **Q1. LLM 코드를 운영 저장소에 바로 쓰지 않은 이유는 무엇인가요?**
  - **답변:** LLM 출력은 비결정적이므로 경로 이탈, 위험 API, 과도한 파일 크기, 브랜드 규칙 위반 가능성이 있습니다. 생성과 반영을 분리하고 허용 루트·경로·import/API 패턴·빌드를 검증한 파일만 워커가 반영했습니다.
  - **파생 Q1-1. 문자열 패턴 검사만으로 위험 코드를 막을 수 있나요?**
    - **답변:** 충분하지 않습니다. 가능하면 AST 기반 import·호출 검사, 샌드박스 빌드, 권한이 제한된 워커를 함께 사용하고 최종 반영 전 사람의 diff 확인을 거쳐야 합니다.

- **Q2. Sandpack과 실제 Next.js 결과의 차이는 어떻게 다뤘나요?**
  - **답변:** Sandpack은 UI 초안 확인에 사용하고 실제 반영 기준으로 삼지 않았습니다. 허용 import와 파일 구조를 검사한 뒤 실제 프로젝트 환경에서 빌드·렌더 검증을 별도로 수행했습니다.
  - **파생 Q2-1. 미리보기에서는 성공하지만 실제 빌드가 실패하는 사례는 무엇인가요?**
    - **답변:** 서버 전용 API, 프로젝트 alias, 환경 변수, 이미지·폰트 설정, 공통 레이아웃 의존성에서 차이가 날 수 있습니다. 그래서 실제 저장소 설정을 사용하는 검증 단계가 필요했습니다.

- **Q3. 생성 결과의 버전과 충돌은 어떻게 관리했나요?**
  - **답변:** 생성 턴마다 입력, 산출물 해시, 대상 경로와 기준 버전을 기록했습니다. 미리보기 중 운영 파일이 바뀌어 해시가 달라지면 자동 덮어쓰지 않고 충돌로 처리했습니다.
  - **파생 Q3-1. 충돌이 발생하면 사용자에게 어떤 선택을 제공했나요?**
    - **답변:** 현재 운영본과 생성본의 diff를 보여주고 다시 생성, 수동 병합, 작업 취소 중 하나를 선택하게 했습니다. 강제 덮어쓰기는 기본 동작에서 제외했습니다.

- **Q4. LLM 호출 실패 시 UX는 어떻게 유지했나요?**
  - **답변:** 대기·생성·검증·실패 상태를 분리하고 재시도 가능한 오류와 입력 수정이 필요한 오류를 구분했습니다. 모델 장애 시 규칙 기반 템플릿으로 기본 랜딩을 만들 수 있게 했습니다.
  - **파생 Q4-1. 자동 재시도는 어떤 경우에만 허용했나요?**
    - **답변:** timeout, 일시적 네트워크 오류, rate limit처럼 같은 입력으로 회복 가능한 경우에 횟수와 간격을 제한해 재시도했습니다. 검증 실패나 잘못된 입력은 자동 재시도하지 않았습니다.

- **Q5. 제작 리드타임 단축 성과는 어떻게 설명하나요?**
  - **답변:** 기존 요청 접수부터 초안 전달까지와 도입 후 같은 유형 작업의 시간을 비교합니다. 개발자 작업 시간, 피드백 횟수, 운영 반영 시간을 분리하고 정확한 로그가 없다면 내부 표본 기준 추정치라고 범위를 한정합니다.
  - **파생 Q5-1. 생산성 외에 품질 저하 여부는 어떻게 확인했나요?**
    - **답변:** 빌드 성공 여부, 브랜드 규칙 위반, 수정 요청 횟수와 운영 반영 후 오류를 함께 확인해야 합니다. 시간 단축만으로 도구의 효과를 판단하지 않습니다.

## 3. TideBuilder — Figma to Email HTML

- **Q1. 테이블 기반 이메일 HTML을 생성한 이유는 무엇인가요?**
  - **답변:** Gmail과 Outlook 등 이메일 클라이언트의 CSS 지원 범위가 다르기 때문입니다. 테이블, 인라인 스타일, 명시적 너비처럼 호환성이 검증된 구조를 우선했습니다.
  - **파생 Q1-1. 이메일 결과는 어떻게 테스트했나요?**
    - **답변:** Gmail·Outlook의 데스크톱과 모바일에서 레이아웃, 이미지, CTA 링크와 대체 텍스트를 확인했습니다. 가능하면 실제 발송 테스트나 이메일 렌더링 테스트 서비스를 함께 사용합니다.

- **Q2. Figma 레이어를 어떤 기준으로 변환했나요?**
  - **답변:** 컷, 버튼, 링크 같은 레이어 네이밍 규칙을 정하고 프레임 트리를 섹션과 CTA로 정규화했습니다. 버튼 행 기준으로 이미지를 나눠 클릭 가능한 테이블 셀로 만들었습니다.
  - **파생 Q2-1. 네이밍 규칙을 지키지 않은 레이어는 어떻게 처리했나요?**
    - **답변:** 자동 추론으로 잘못된 결과를 만들기보다 검증 오류와 대상 레이어를 보여주고 수정하도록 했습니다. 일부 선택 항목에만 안전한 기본값을 적용했습니다.

- **Q3. Figma API 호출 제한은 어떻게 대응했나요?**
  - **답변:** 동시성을 제한하고 지수 백오프로 재시도했습니다. REST API가 불안정한 경우 Figma 플러그인 입력으로 전환하되 이후 파싱·슬라이싱 로직은 공유했습니다.
  - **파생 Q3-1. 재시도로 중복 작업이 생기지 않게 어떻게 했나요?**
    - **답변:** 프레임과 작업 단위 식별자를 두고 이미 완료한 결과를 재사용했습니다. 업로드도 같은 객체 키나 작업 상태를 확인한 뒤 진행했습니다.

- **Q4. 대용량 업로드 실패는 어떻게 해결했나요?**
  - **답변:** 큰 데이터를 애플리케이션 서버 JSON으로 전달하지 않고 압축·청크 처리 후 서명 URL로 스토리지에 직접 업로드했습니다. 서버에는 객체 URL과 메타데이터만 전달했습니다.
  - **파생 Q4-1. 서명 URL 사용 시 보안상 무엇을 제한해야 하나요?**
    - **답변:** 만료 시간, 파일 크기, MIME, 저장 경로와 업로드 메서드를 제한해야 합니다. 업로드 완료 후 서버가 객체를 다시 검증하고 임의 공개를 막아야 합니다.

## 4. 패키지 맞춤여행 견적 문의 게시판

- **Q1. Server Action을 사용한 이유는 무엇인가요?**
  - **답변:** 회원 쿠키와 내부 API 호출을 서버에 모으기 위해 사용했습니다. 각 요청에서 회원 세션과 문의 소유권 또는 비회원 비밀번호를 다시 확인하고 오류 유형을 구분해 반환했습니다.
  - **파생 Q1-1. Server Action도 외부에서 직접 호출될 수 있나요?**
    - **답변:** 가능합니다. UI를 거친다는 전제로 신뢰하면 안 되며 일반 API와 동일하게 입력 검증, 인증, 권한 검사와 rate limit을 적용해야 합니다.

- **Q2. 회원·비회원 접근 제어는 어떻게 설계했나요?**
  - **답변:** 회원 본인·타 회원·비회원 인증 완료·미인증 상태를 명시적으로 나눴습니다. 프론트는 모달과 이동만 담당하고 상세 데이터 권한은 서버가 최종 판단했습니다.
  - **파생 Q2-1. 권한 상태가 늘어나면 조건문이 복잡해지지 않나요?**
    - **답변:** 상태와 허용 액션을 표로 정의하고 `canRead`, `canEdit` 같은 정책 함수로 분리합니다. UI와 API가 같은 정책 기준을 사용하되 서버 판단을 최종값으로 둡니다.

- **Q3. 브라우저 세션의 AES 비밀번호 보관은 안전한가요?**
  - **답변:** AES 값은 같은 탭에서 재입력을 줄이는 UX 캐시일 뿐 권한 경계가 아닙니다. 더 안전한 방식은 서버 검증 후 짧게 만료되는 opaque 토큰을 HttpOnly·Secure·SameSite 쿠키로 발급하는 것입니다.
  - **파생 Q3-1. 클라이언트 암호화가 보안 경계가 될 수 없는 이유는 무엇인가요?**
    - **답변:** 복호화에 필요한 코드와 키가 클라이언트에 함께 존재해 XSS나 개발자 도구 접근을 막을 수 없기 때문입니다. 실제 권한은 서버가 보유한 정보로 검증해야 합니다.

- **Q4. PC 페이징과 모바일 더보기를 한 API로 어떻게 처리했나요?**
  - **답변:** 같은 page·size 계약을 사용하되 PC는 결과를 교체하고 모바일은 다음 페이지를 누적했습니다. 검색 조건 변경 시 누적 목록을 초기화하고 문의 ID로 중복을 제거했습니다.
  - **파생 Q4-1. 요청 순서가 뒤바뀌면 어떻게 처리하나요?**
    - **답변:** 조건별 요청 식별자나 AbortController로 이전 요청을 취소하고 현재 조건과 일치하는 응답만 반영합니다. 모바일 누적도 페이지 순서를 확인한 뒤 병합합니다.

- **Q5. 긴 작성 폼의 사용성을 어떻게 높였나요?**
  - **답변:** 필드별 검증과 제출 시 전체 검증을 조합하고 첫 오류로 스크롤·포커스했습니다. 표시값과 전송값을 분리하고 장애 복원 데이터에서는 개인정보를 제외했습니다.
  - **파생 Q5-1. 서버 검증 오류는 폼에 어떻게 연결했나요?**
    - **답변:** 필드 오류와 전역 오류를 구분해 필드 오류는 입력 근처에 표시하고 첫 오류로 포커스했습니다. 중복 제출과 서버 처리 중 상태도 버튼에 명확히 표시했습니다.

## 5. 투어비스 USJ AI 챗봇 UI

- **Q1. FAQ와 AI 응답을 결합한 이유는 무엇인가요?**
  - **답변:** 빈도가 높고 정확성이 중요한 질문은 FAQ로 즉시 답하고, 매칭되지 않는 자유 질의만 AI로 확장했습니다. 응답 속도와 질문 범위를 함께 확보하기 위한 선택이었습니다.
  - **파생 Q1-1. FAQ 매칭 임계값은 어떻게 정하나요?**
    - **답변:** 실제 질문 표본으로 오탐과 미탐을 비교해 정합니다. 확신도가 애매하면 잘못된 FAQ를 보여주기보다 AI나 추가 질문으로 전환합니다.

- **Q2. 스트리밍 응답 상태는 어떻게 관리했나요?**
  - **답변:** 사용자 전송, 서버 접수, 스트림 수신, 완료, 실패를 분리하고 처리 중 중복 전송을 막았습니다. chunk를 버퍼에 누적해 이벤트 단위로 파싱했습니다.
  - **파생 Q2-1. 사용자가 스트리밍 중 취소하면 어떻게 처리하나요?**
    - **답변:** AbortController로 연결을 종료하고 현재까지 받은 텍스트는 유지합니다. 메시지는 취소 상태로 표시하고 다음 질문을 보낼 수 있게 요청 상태를 정리합니다.

- **Q3. 스트리밍 텍스트의 XSS는 어떻게 막았나요?**
  - **답변:** 일반 텍스트는 escape하고 볼드·줄바꿈처럼 허용한 마크업만 파서로 변환했습니다. 모델 출력을 그대로 `dangerouslySetInnerHTML`에 전달하지 않았습니다.
  - **파생 Q3-1. 링크를 지원한다면 어떤 검증이 필요한가요?**
    - **답변:** `http/https` 프로토콜만 허용하고 표시 텍스트와 URL을 escape합니다. 새 창 링크에는 `noopener noreferrer`를 적용하고 의심 URL은 렌더하지 않습니다.

- **Q4. PC와 모바일 UX를 왜 다르게 구성했나요?**
  - **답변:** PC는 상품 페이지 맥락을 유지하는 플로팅 패널, 모바일은 대화와 키보드에 집중하는 풀스크린으로 구성했습니다. 메시지와 세션 로직은 공통으로 유지했습니다.
  - **파생 Q4-1. 모바일 가상 키보드 문제는 어떻게 대응했나요?**
    - **답변:** 동적 viewport와 safe area를 고려하고 입력창을 고정했습니다. 열고 닫을 때 배경 스크롤 위치를 보존하고 최신 메시지가 가려지지 않는지 확인했습니다.

## 6. 마케팅 Vercel 앱 프록시 및 SEO

- **Q1. redirect가 아니라 rewrite를 사용한 이유는 무엇인가요?**
  - **답변:** 본 도메인 URL과 canonical, 분석 흐름을 유지하면서 캠페인 앱은 독립적으로 배포하기 위해서입니다. 사용자는 본 도메인을 유지하고 내부적으로 독립 앱의 응답을 받습니다.
  - **파생 Q1-1. rewrite 구조의 장애 범위는 어떻게 줄이나요?**
    - **답변:** 캠페인별 timeout과 fallback을 두고 원본 앱 장애가 메인 앱 전체로 번지지 않게 합니다. 라우팅 맵 변경도 단계적으로 배포하고 상태를 모니터링합니다.

- **Q2. 정적 자산 경로 문제는 어떻게 처리하나요?**
  - **답변:** HTML 경로뿐 아니라 `/_next/static`, 이미지, API 요청도 앱별 base path나 asset prefix와 맞췄습니다. 쿠키 범위, 캐시 헤더와 404도 함께 검증했습니다.
  - **파생 Q2-1. 서로 다른 앱의 chunk 이름이 충돌할 수 있나요?**
    - **답변:** 같은 자산 경로를 공유하면 충돌할 수 있습니다. 앱별 경로 prefix와 빌드 ID를 분리하고 HTML이 참조하는 자산 origin을 일관되게 유지해야 합니다.

- **Q3. 중복 콘텐츠는 어떻게 방지했나요?**
  - **답변:** 본 도메인 URL을 canonical로 고정하고 sitemap에는 공개 경로만 포함했습니다. OG URL, 구조화 데이터와 내부 링크도 같은 URL 유틸을 사용했습니다.
  - **파생 Q3-1. canonical만 지정하면 중복 문제가 모두 해결되나요?**
    - **답변:** 검색 엔진에 주는 힌트이므로 충분하지 않습니다. 원본 도메인 인덱싱 제한, 내부 링크 통일, sitemap 정리와 응답 상태까지 함께 관리해야 합니다.

- **Q4. SEO 메타는 어떻게 표준화했나요?**
  - **답변:** 브랜드 공통 기본값과 경로별 덮어쓰기 구조를 만들고 title, description, canonical, OG와 sitemap이 같은 설정을 참조하게 했습니다. AI 초안은 사람이 검토했습니다.
  - **파생 Q4-1. 메타 병합에서 배열이나 빈 값은 어떻게 처리했나요?**
    - **답변:** 단순 얕은 병합을 피하고 필드별 정책을 정의했습니다. 빈 값은 기본값 삭제인지 미설정인지 구분하고 OG 이미지처럼 배열인 값은 교체와 추가 규칙을 명확히 했습니다.

## 7. 투어비스 고객센터 — Zendesk Guide

- **Q1. 레거시 JavaScript를 어떻게 개선했나요?**
  - **답변:** 전면 교체 대신 인증, 예약 조회, 로더, 오류, 모달, 무한 스크롤의 역할을 분리했습니다. DOM 접근 지점을 모으고 이벤트 등록·해제를 대칭으로 관리했습니다.
  - **파생 Q1-1. 점진적 개선의 우선순위는 어떻게 정했나요?**
    - **답변:** 장애 빈도, 변경 빈도와 다른 기능에 미치는 영향을 기준으로 정했습니다. 공통 로더·오류·모달처럼 여러 화면이 쓰는 영역부터 분리했습니다.

- **Q2. 예약 기반 문의 흐름은 어떻게 구현했나요?**
  - **답변:** 예약 선택 후 카테고리, 섹션, 예약번호를 문의 폼으로 전달했습니다. 접수 서버는 URL 값을 그대로 신뢰하지 않고 로그인 세션과 예약 소유권을 다시 검증했습니다.
  - **파생 Q2-1. 딥링크 값이 변조되면 어떻게 되나요?**
    - **답변:** 서버 검증에 실패하면 예약 정보를 접수 데이터에 포함하지 않고 권한 오류를 반환합니다. 클라이언트의 hidden field나 query 값은 신뢰하지 않습니다.

- **Q3. 무한 스크롤에서 어떤 문제를 고려했나요?**
  - **답변:** observer 중복 호출, 빠른 스크롤의 중복 요청과 모달 종료 후 이벤트 누수를 처리했습니다. 예약 ID로 결과를 중복 제거하고 종료·빈 결과·오류 상태를 분리했습니다.
  - **파생 Q3-1. Intersection Observer를 지원하지 않는 환경은 어떻게 하나요?**
    - **답변:** 명시적인 더 보기 버튼을 fallback으로 제공합니다. 자동 로드는 보조 기능으로 두면 접근성과 호환성을 함께 확보할 수 있습니다.

- **Q4. 웹과 앱 WebView 차이는 어떻게 다뤘나요?**
  - **답변:** user agent만 보지 않고 bridge 제공 여부를 확인했습니다. 로그인, 외부 링크, 뒤로 가기와 채팅 진입을 adapter로 감싸고 웹에서는 URL 이동으로 폴백했습니다.
  - **파생 Q4-1. bridge 호출 실패는 어떻게 처리하나요?**
    - **답변:** 호출 가능 여부와 timeout을 확인하고 실패 시 웹 동작으로 전환합니다. 사용자에게 아무 반응이 없는 상태가 되지 않도록 오류나 대체 이동을 제공합니다.

## 8. 다음 쇼핑 제휴 랜딩

- **Q1. CMS 응답을 바로 컴포넌트에 전달하지 않은 이유는 무엇인가요?**
  - **답변:** CMS 스키마 변경이 UI 전체로 퍼지는 것을 막기 위해 템플릿별 adapter에서 공통 UI 모델로 변환했습니다. 누락 필드는 경계에서 검증했습니다.
  - **파생 Q1-1. 정규화 계층의 단점은 무엇인가요?**
    - **답변:** 변환 코드가 추가되고 원본 데이터 추적이 어려워질 수 있습니다. 변환 함수와 타입을 단순하게 유지하고 원본 식별자를 로그에 남겨 디버깅 가능성을 확보합니다.

- **Q2. 템플릿 레지스트리는 어떻게 확장했나요?**
  - **답변:** 템플릿 ID와 렌더 컴포넌트, 변환기, fallback을 레지스트리로 관리했습니다. 신규 섹션은 조건문을 늘리지 않고 항목을 추가하도록 했습니다.
  - **파생 Q2-1. 알 수 없는 템플릿 ID가 오면 어떻게 하나요?**
    - **답변:** 전체 페이지를 실패시키지 않고 해당 섹션을 건너뛰거나 안전한 fallback을 표시합니다. 동시에 템플릿 ID와 응답을 모니터링에 남깁니다.

- **Q3. 지연 렌더링의 단점은 어떻게 보완했나요?**
  - **답변:** 첫 화면과 SEO 핵심 콘텐츠는 즉시 렌더하고 무거운 하위 섹션만 지연했습니다. 예상 높이와 root margin을 사용해 빈 영역과 CLS를 줄였습니다.
  - **파생 Q3-1. 지연 로드가 오히려 느리게 느껴지는 경우는 언제인가요?**
    - **답변:** 사용자가 빠르게 스크롤하거나 네트워크가 느린데 로드를 너무 늦게 시작할 때입니다. 실제 사용 데이터를 기준으로 root margin과 우선순위를 조정해야 합니다.

- **Q4. ISR은 어떤 기준으로 사용했나요?**
  - **답변:** 매 요청 실시간일 필요는 없지만 운영 변경이 일정 시간 안에 반영되어야 하는 전시 데이터에 사용했습니다. 즉시 반영이 필요하면 on-demand revalidation을 고려합니다.
  - **파생 Q4-1. 가격·재고도 같은 캐시를 사용하나요?**
    - **답변:** 변경 민감도가 높아 같은 주기를 사용하면 안 됩니다. 짧은 서버 캐시나 클라이언트 재조회로 분리하고 화면에 데이터 기준 시점을 명확히 해야 합니다.

## 9. airspot 백오피스 UI/UX 안정화

- **Q1. 플러그인 스타일 충돌을 어떻게 조사했나요?**
  - **답변:** computed style과 적용 순서를 확인해 전역 selector, breakpoint, specificity 문제를 분리했습니다. 전체 override 대신 백오피스 root scope와 CSS Module에서 필요한 규칙만 보정했습니다.
  - **파생 Q1-1. `!important`로 빠르게 해결하지 않은 이유는 무엇인가요?**
    - **답변:** 우선순위 경쟁을 더 키워 이후 수정과 상태별 스타일을 어렵게 만들기 때문입니다. selector 범위와 로드 순서를 바로잡는 것을 우선했습니다.

- **Q2. JavaScript 분기와 media query 중 무엇을 선택하나요?**
  - **답변:** 같은 콘텐츠의 배치·노출 차이는 CSS로 처리하고 데이터나 상호작용 자체가 다를 때만 JavaScript를 사용합니다. 불필요한 hydration 불일치와 resize 상태 관리를 피하기 위해서입니다.
  - **파생 Q2-1. CSS로 숨긴 콘텐츠가 두 번 렌더되는 문제는 없나요?**
    - **답변:** DOM을 PC·모바일로 중복 작성하지 않고 가능한 한 하나의 의미 구조를 재배치합니다. 정말 다른 UI만 분리하고 접근성 트리와 데이터 요청 중복을 확인합니다.

- **Q3. 최소 변경으로 안정화한 이유는 무엇인가요?**
  - **답변:** 운영 중인 백오피스의 전면 교체는 회귀 위험과 검증 범위가 컸습니다. 영향 범위를 격리한 스타일 보정으로 결함을 먼저 해결하고 장기 개선 과제를 분리했습니다.
  - **파생 Q3-1. 언제 전면 리팩터링을 선택해야 하나요?**
    - **답변:** 보정 코드가 반복되고 플러그인 제약 때문에 신규 요구를 구현하기 어렵거나 장애 비용이 교체 비용을 넘을 때입니다. 사용 화면과 위험도를 나눠 단계적으로 전환합니다.

## 10. 투어비스 마이페이지 리뉴얼

- **Q1. 서버와 클라이언트 컴포넌트를 어떻게 나눴나요?**
  - **답변:** 회원 확인, 초기 데이터와 정적인 레이아웃은 서버에서 처리했습니다. 탭, 필터, 추가 조회, 무한 스크롤, 모달처럼 브라우저 상태가 필요한 부분만 클라이언트로 분리했습니다.
  - **파생 Q1-1. 클라이언트 경계가 너무 커지면 어떤 문제가 생기나요?**
    - **답변:** 전송 JavaScript와 hydration 범위가 늘고 서버 전용 코드 분리가 어려워집니다. 이벤트가 필요한 가장 작은 단위에 경계를 두고 직렬화 가능한 데이터만 전달합니다.

- **Q2. 상품군별 예약 카드는 어떻게 공통화했나요?**
  - **답변:** 공통 카드 shell과 상품군별 adapter를 분리해 예약 데이터를 공통 view model로 변환했습니다. 상품별 예외는 명시적인 variant로 표현했습니다.
  - **파생 Q2-1. variant가 계속 늘어나면 어떻게 하나요?**
    - **답변:** 공통화 기준을 다시 검토합니다. 정책이 크게 다른 상품군은 별도 컴포넌트로 분리하고 레이아웃·타이포그래피 같은 안정적인 부분만 공유합니다.

- **Q3. 쿠폰 뱃지 정책을 어떻게 동기화했나요?**
  - **답변:** 선착순, 잔여, 마감 임박 조건을 순수 함수와 공통 타입으로 분리해 쿠폰함과 결제 모달이 같은 결과를 사용하게 했습니다.
  - **파생 Q3-1. 날짜 경계값은 어떻게 테스트하나요?**
    - **답변:** 기준 시간을 주입할 수 있게 만들고 시작 직전·정각·종료 직전·종료 후를 테이블 테스트합니다. 서버와 브라우저의 timezone 기준도 통일합니다.

- **Q4. 무한 스크롤 접근성은 어떻게 보완하나요?**
  - **답변:** 더 보기 버튼을 기본 조작으로 제공하고 Intersection Observer는 보조 수단으로 사용합니다. 추가 항목 수와 목록 종료를 live region으로 안내합니다.
  - **파생 Q4-1. 항목 추가 후 focus를 이동해야 하나요?**
    - **답변:** 자동 로드에서는 기존 읽기 위치를 유지하고, 버튼으로 요청한 경우에도 보통 버튼 위치를 유지합니다. 새 콘텐츠 추가 사실을 안내하되 예상치 못한 focus 이동은 피합니다.

## 11. 투어비스 공통 메인·GNB 리뉴얼

- **Q1. GNB를 공유 패키지로 만든 이유는 무엇인가요?**
  - **답변:** 앱별 중복 구현과 정책 불일치를 줄이기 위해 UI, 타입, 데이터 정규화와 이벤트 계약을 공유 패키지로 묶었습니다.
  - **파생 Q1-1. 공유 패키지가 앱별 요구를 막지는 않았나요?**
    - **답변:** 공통 정책과 레이아웃은 패키지가 제공하고 앱 차이는 데이터와 제한된 slot·variant로 주입했습니다. 앱 전용 조건이 공통 코드에 퍼지지 않게 했습니다.

- **Q2. 환경마다 다른 메뉴 데이터는 어떻게 안정화했나요?**
  - **답변:** CMS 원본을 정규화 계층에서 공통 메뉴 모델로 바꾸고 URL, 타입, children을 검증했습니다. 렌더러는 정규화된 UX 유형만 사용했습니다.
  - **파생 Q2-1. 런타임 검증 실패 시 GNB 전체가 사라지나요?**
    - **답변:** 잘못된 항목만 제외하고 핵심 메뉴 fallback을 표시합니다. 오류 위치와 원본 식별자를 기록해 운영 데이터 문제를 추적합니다.

- **Q3. 서버와 클라이언트 역할은 어떻게 분리했나요?**
  - **답변:** feature flag, 회원 상태와 메뉴 원본은 서버에서 준비하고 hover, focus, 모달과 모바일 메뉴만 클라이언트로 분리했습니다.
  - **파생 Q3-1. 서버에서 회원 정보를 어디까지 내려주나요?**
    - **답변:** 메뉴 분기에 필요한 최소 상태만 전달하고 토큰이나 불필요한 개인정보는 제외합니다. 실제 권한이 필요한 동작은 서버에서 다시 검증합니다.

- **Q4. 배너 셔플의 hydration 불일치는 어떻게 막나요?**
  - **답변:** 서버와 클라이언트가 각각 무작위 순서를 만들지 않고 서버에서 순서를 확정하거나 seed 기반 결정적 셔플을 사용합니다.
  - **파생 Q4-1. 랜덤 노출의 공정성은 어떻게 측정하나요?**
    - **답변:** 배너별 impression과 사용자 단위 반복 노출을 기록해 같은 우선순위의 노출 분포를 비교합니다. 단순 호출 횟수보다 실제 노출 이벤트를 사용합니다.

- **Q5. 공유 패키지의 장애 전파 위험은 어떻게 줄이나요?**
  - **답변:** 단위·시각 회귀·소비 앱 smoke test 후 특정 앱에 먼저 적용합니다. breaking change는 버전으로 분리하고 lockfile로 일괄 업데이트를 막습니다.
  - **파생 Q5-1. 긴급 장애 시 어떻게 롤백하나요?**
    - **답변:** 소비 앱이 이전 패키지 버전으로 즉시 돌아갈 수 있게 버전을 고정하고 배포 이력을 유지합니다. 최소 내비 fallback으로 사용자 이동도 보장합니다.

## 12. 투어비스 항공 리뉴얼 퍼블리싱

- **Q1. 디자인 시안 없이 구현 기준을 어떻게 만들었나요?**
  - **답변:** 운영 화면을 헤더, 검색, 여정 카드, 운임과 상태 UI 패턴으로 분해했습니다. 정보 우선순위와 변경 범위를 담당자와 합의하고 대표 화면을 기준으로 만들었습니다.
  - **파생 Q1-1. 구두 합의가 바뀌는 문제는 어떻게 막았나요?**
    - **답변:** 대표 화면, 상태별 캡처와 반응형 규칙을 문서로 남기고 변경 요청의 영향 범위를 함께 기록했습니다.

- **Q2. FE가 사용하기 쉬운 퍼블리싱 산출물은 무엇인가요?**
  - **답변:** DOM이 데이터 반복 구조와 대응하고 상태 class와 컴포넌트 경계가 예측 가능해야 합니다. 빈 상태, 오류, 긴 텍스트 같은 예외 샘플도 제공합니다.
  - **파생 Q2-1. 퍼블리셔와 FE의 책임 경계는 어떻게 정했나요?**
    - **답변:** 퍼블리싱은 의미 구조, 반응형 스타일과 UI 상태 샘플을 제공하고 FE는 실제 데이터·상태·이벤트를 연결했습니다. 상태 이름과 마크업 계약을 먼저 합의했습니다.

- **Q3. 항공 검색 UI 접근성에서 중요한 점은 무엇인가요?**
  - **답변:** 출발지·도착지, 날짜, 인원 같은 복합 입력의 label·오류 연결과 달력·모달의 키보드 이동, focus 복원이 중요합니다.
  - **파생 Q3-1. 동적 검색 결과는 어떻게 안내하나요?**
    - **답변:** 결과 개수와 갱신 상태를 live region으로 알리고 목록은 키보드로 탐색할 수 있게 합니다. 선택 후 입력으로 focus가 자연스럽게 돌아가야 합니다.

## 13. 투어비스 리뉴얼 퍼블리싱

- **Q1. 모바일 퍼스트를 선택한 이유는 무엇인가요?**
  - **답변:** 작은 화면에서 핵심 콘텐츠와 조작 우선순위를 먼저 정하고 넓은 화면에서 컬럼과 보조 정보를 확장하기 위해서입니다.
  - **파생 Q1-1. 모바일 퍼스트가 항상 적합한가요?**
    - **답변:** 아닙니다. 데이터 밀도가 높은 관리자나 데스크톱 중심 도구는 핵심 사용 환경을 먼저 설계할 수 있습니다. 중요한 것은 기기명이 아니라 사용자와 콘텐츠 제약입니다.

- **Q2. PC·모바일 페이지를 어떻게 단일화했나요?**
  - **답변:** 콘텐츠와 동작이 같은 영역은 하나의 의미 구조와 반응형 스타일로 합치고 정말 다른 UX만 variant로 분리했습니다.
  - **파생 Q2-1. 단일 컴포넌트의 조건문이 많아지면 어떻게 하나요?**
    - **답변:** 공통 데이터 모델은 유지하되 표현 컴포넌트를 분리합니다. 중복 제거보다 읽기 쉬운 경계와 독립적인 변경 가능성을 우선합니다.

- **Q3. breakpoint는 어떻게 정했나요?**
  - **답변:** 특정 기기 목록이 아니라 검색 폼, 카드 최소 너비와 내비 공간처럼 콘텐츠가 깨지는 지점을 기준으로 정했습니다.
  - **파생 Q3-1. 중간 화면 폭은 어떻게 검증하나요?**
    - **답변:** 정의된 breakpoint만 확인하지 않고 그 사이 폭과 브라우저 확대 상태를 점검합니다. 컨테이너가 실제로 깨지는 구간을 찾아 규칙을 조정합니다.

## 14. 투어비스 호텔 메인 UI 리뉴얼

- **Q1. 페이지를 얇게 유지한 이유는 무엇인가요?**
  - **답변:** 페이지는 데이터 조회와 섹션 순서만 담당하고 템플릿 ID를 독립 섹션 컴포넌트에 매핑했습니다. 신규 전시 추가와 변경 범위를 줄이기 위한 구조였습니다.
  - **파생 Q1-1. 섹션 간 공통 상태는 어떻게 공유하나요?**
    - **답변:** 서버에서 필요한 공통 데이터를 한 번 정규화해 props로 전달하고, 브라우저 상호작용 상태만 가까운 공통 부모나 제한된 context에서 관리합니다.

- **Q2. 첫 화면과 하단 섹션의 로딩 전략은 어떻게 달랐나요?**
  - **답변:** 상단 내비와 첫 콘텐츠는 즉시 렌더하고 크기가 정해진 스켈레톤을 사용했습니다. 개인화 영역과 하단 섹션은 지연 로드했습니다.
  - **파생 Q2-1. CLS는 어떻게 줄였나요?**
    - **답변:** 이미지와 섹션의 예상 크기를 미리 확보하고 스켈레톤도 최종 레이아웃과 비슷하게 만들었습니다. 데이터 도착 후 상단 높이가 바뀌지 않게 했습니다.

- **Q3. SSR과 CSR의 경계는 무엇이었나요?**
  - **답변:** Pages Router의 SSR에서 CMS 데이터와 기본 섹션 마크업을 만들고 탭, 검색, 드롭다운, Swiper처럼 브라우저 상태가 필요한 기능만 CSR로 처리했습니다.
  - **파생 Q3-1. 모든 섹션을 SSR하면 서버 부하가 커지지 않나요?**
    - **답변:** 데이터 변경 주기와 개인화 여부에 따라 캐시 가능한 전시는 캐시하고 사용자별 데이터만 요청 시 처리합니다. 무거운 하단 상호작용은 지연 로드합니다.

- **Q4. 캠페인을 탭과 배지만으로 전환한 장점은 무엇인가요?**
  - **답변:** 별도 페이지를 복제하지 않고 설정과 일부 UI만 변경해 배포·원복 범위를 줄일 수 있었습니다.
  - **파생 Q4-1. 캠페인 상태를 URL에 넣어야 하는 경우는 언제인가요?**
    - **답변:** 사용자가 특정 탭을 공유하거나 검색·분석에서 별도 랜딩으로 구분해야 할 때입니다. 단순 기간성 기본 상태라면 설정으로 처리할 수 있습니다.

## 15. 투어비스 Publish — Gulp 빌드 환경

- **Q1. 당시 Gulp를 선택한 이유는 무엇인가요?**
  - **답변:** 기존 정적 산출물 구조를 유지하면서 SCSS, HTML include, JS 압축, 이미지 최적화와 로컬 서버를 빠르게 통일하기에 적합했습니다.
  - **파생 Q1-1. 지금 다시 만든다면 무엇을 선택하나요?**
    - **답변:** 요구가 정적 퍼블리싱인지 애플리케이션인지 먼저 구분하고 Vite나 프레임워크 빌드를 검토합니다. 기존 배포 계약과 팀 학습 비용까지 비교해 결정합니다.

- **Q2. Gulp 태스크를 어떻게 구성했나요?**
  - **답변:** 스타일, 마크업, 스크립트, 이미지를 독립 태스크로 만들고 의존 작업은 series, 독립 작업은 parallel로 실행했습니다. 각 stream의 종료와 오류를 명확히 반환했습니다.
  - **파생 Q2-1. watch 중 오류로 프로세스가 종료되는 문제는 어떻게 다루나요?**
    - **답변:** 개발 watch에서는 오류를 표시하고 해당 stream을 복구하되 production build에서는 실패 코드를 반환해 잘못된 산출물이 배포되지 않게 합니다.

- **Q3. 변경된 이미지만 최적화한 이유는 무엇인가요?**
  - **답변:** 전체 이미지를 매번 처리하는 비용을 줄여 개발 피드백을 빠르게 하기 위해서입니다. 클린 빌드에서는 전체 결과를 다시 검증했습니다.
  - **파생 Q3-1. 삭제된 원본 이미지의 산출물은 어떻게 처리하나요?**
    - **답변:** 증분 복사만 하면 오래된 파일이 남으므로 clean 단계나 원본과 산출물 동기화 단계에서 삭제합니다. 배포 빌드는 깨끗한 디렉터리에서 생성하는 것이 안전합니다.

- **Q4. HTML partial include의 장점과 한계는 무엇인가요?**
  - **답변:** 공통 마크업의 복사·누락을 줄이지만 타입 검사와 복잡한 상태 표현에는 한계가 있습니다. 단순 헤더·푸터에 제한하고 애플리케이션 로직은 컴포넌트로 옮깁니다.
  - **파생 Q4-1. include 의존성이 깊어지면 어떻게 관리하나요?**
    - **답변:** 중첩 깊이를 제한하고 공통 조각의 입력값과 사용 위치를 문서화합니다. 조건과 데이터가 많아지면 템플릿 도구나 프레임워크 컴포넌트로 전환합니다.

## 16. 프로젝트 공통 심층 질문

- **Q1. 혼자 프론트엔드를 담당할 때 코드 품질을 어떻게 보장했나요?**
  - **답변:** 요구사항, API 계약과 상태 모델을 문서화하고 lint, type check, build와 핵심 플로우 테스트를 완료 기준으로 사용했습니다. 중간 결과를 관련 직군과 자주 검증했습니다.
  - **파생 Q1-1. 코드 리뷰어가 없으면 무엇으로 보완하나요?**
    - **답변:** 작은 변경 단위, 체크리스트, 정적 분석과 자동 테스트로 기본 품질을 확보하고 위험한 설계는 백엔드나 다른 개발자에게 별도 검토를 요청합니다.

- **Q2. 재사용성과 프로젝트 요구가 충돌하면 어떻게 판단하나요?**
  - **답변:** 시각적 유사성보다 데이터 구조, 상호작용과 변경 주기가 같은지 확인합니다. 공통 shell과 정책별 adapter·variant를 분리합니다.
  - **파생 Q2-1. 잘못된 공통화의 신호는 무엇인가요?**
    - **답변:** 조건 prop이 계속 늘고 한 프로젝트 수정이 다른 프로젝트 회귀를 만들거나 공통 컴포넌트가 도메인 예외를 알기 시작하는 경우입니다.

- **Q3. 성능 최적화는 어떤 순서로 하나요?**
  - **답변:** 사용자 지표와 프로파일링으로 병목을 확인한 뒤 요청, JavaScript, 이미지, 렌더 차단 리소스와 반복 렌더 순으로 영향이 큰 항목을 줄입니다.
  - **파생 Q3-1. 최적화가 효과가 없으면 어떻게 하나요?**
    - **답변:** 변경 전후를 같은 조건에서 다시 측정하고 가설이 틀렸다면 되돌립니다. 점수 개선보다 실제 사용자 경로의 체감과 오류 여부를 우선합니다.

- **Q4. 프론트엔드 오류 처리는 어떻게 설계하나요?**
  - **답변:** 입력, 인증·권한, 일시적 네트워크와 서버 오류를 구분합니다. 해결 가능한 오류는 행동 근처에 안내하고 예상하지 못한 오류는 모니터링하되 개인정보를 제거합니다.
  - **파생 Q4-1. 모든 네트워크 오류를 재시도해도 되나요?**
    - **답변:** 아닙니다. 조회나 idempotency가 보장된 요청만 제한적으로 재시도하고 결제·등록 같은 변경 요청은 중복 처리 방지 키와 서버 상태를 확인해야 합니다.

- **Q5. 접근성을 일정 안에서 어떻게 확보했나요?**
  - **답변:** 접근성을 마지막 QA가 아니라 공통 컴포넌트의 기본 조건으로 넣었습니다. 시맨틱 요소, label, 키보드, focus와 상태 표현을 먼저 해결하고 주요 여정을 수동 점검했습니다.
  - **파생 Q5-1. 자동 접근성 검사만으로 충분한가요?**
    - **답변:** 충분하지 않습니다. 자동 도구는 일부 규칙만 찾으므로 키보드 순서, 스크린리더 문맥, 모달 focus와 실제 업무 흐름을 수동으로 확인해야 합니다.

- **Q6. 본인의 기여도를 어떻게 설명하나요?**
  - **답변:** 담당 인원보다 직접 결정한 구조, 구현 범위, 다른 직군과 합의한 계약과 검증 결과를 분리해 설명합니다. 팀 성과와 개인 기여도도 구분합니다.
  - **파생 Q6-1. 결과가 팀 성과인데 본인 성과처럼 보이지 않게 하려면 어떻게 하나요?**
    - **답변:** “팀이 달성한 결과”와 “그중 제가 맡은 결정·구현”을 문장에서 분리합니다. 코드, 문서, 이슈처럼 확인 가능한 근거를 함께 제시합니다.
